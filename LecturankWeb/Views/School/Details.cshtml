@model LecturankWeb.Models.SchoolDetailsViewModel

<div class="school-profile">
    <!-- Image Slider -->
    <div class="image-slider">
        <div class="slide active">
            <img src="https://tribuneonlineng.com/wp-content/uploads/2016/08/futa-gate.jpg" alt="Photo 1">
        </div>
        <div class="slide">
            <img src="https://www.nairaland.com/attachments/11669471_91c89bb464354869ae95105da911b95b_jpeg61f8a0f5db5b3f7b76c760ccc386fa3d" alt="Photo 2">
        </div>
        <div class="slide">
            <img src="https://www.nairaland.com/attachments/11669586_images12_jpeg_jpeg9bd644d4a5050c34dc065d7f8f336c3f" alt="Photo 3">
        </div>
        <div class="slide">
            <img src="https://www.nairaland.com/attachments/11669617_images13_jpeg_jpegc38cacc753be0ec722c575388df668f6" alt="Photo 4">
        </div>
        <div class="slide">
            <img src="https://www.nairaland.com/attachments/11669619_images15_jpeg_jpegae88086c12dcb82a59e4407c891bbff6" alt="Photo 5">
        </div>
        <!-- Left and right controls -->
        <a class="prev" onclick="plusSlides(-1)">&#10094;</a>
        <a class="next" onclick="plusSlides(1)">&#10095;</a>
    </div>

    <!-- School Details Section -->
    <h1 class="school-name">@Model.Name</h1>
    <p class="school-address">@Model.Address</p>

    <div class="about">
        <h3>Overview</h3>
        <p>@Model.Description</p>
    </div>

    <div class="amenities">
        <h3>Amenities</h3>
        <div class="amenities-grid">
            @foreach (var amenity in Model.Amenities)
            {
                <div class="amenity-item">
                    <img src="https://i.postimg.cc/HW2h3CkB/Todo1.jpg" alt="Amenity Icon" />
                    <span>@amenity</span>
                </div>
            }
        </div>
    </div>

    <div class="reviews">
        <h3>Reviews</h3>
        @foreach (var review in Model.Reviews)
        {
            <div class="review">
                <p>@review.Content</p>
                <small>Reviewer: @review.ReviewerName | Rating: 
                    @for (int i = 0; i < 5; i++)
                    {
                        if (i < (int)review.Rating)
                        {
                            @:&#9733; <!-- Filled star -->
                        }
                        else
                        {
                            @:&#9734; <!-- Empty star -->
                        }
                    }
                </small>
            </div>
        }
    </div>
</div>

<!-- Back Button -->
<div class="mt-3">
    <button onclick="window.history.back()" class="btn btn-primary">Back</button>
</div>

@section Scripts {
    <script>
        let slideIndex = 0;
        showSlides(slideIndex);

        function plusSlides(n) {
            showSlides(slideIndex += n);
        }

        function showSlides(n) {
            let slides = document.getElementsByClassName("slide");
            if (n >= slides.length) {
                slideIndex = 0;
            }
            if (n < 0) {
                slideIndex = slides.length - 1;
            }
            for (let i = 0; i < slides.length; i++) {
                slides[i].style.display = "none";
            }
            slides[slideIndex].style.display = "block";
        }
    </script>
}
